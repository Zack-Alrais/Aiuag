{
  "version": 2,
  "buildCommand": "npx prisma db push --accept-data-loss && npx prisma generate && next build",
  "installCommand": "npm install",
  "outputDirectory": ".next",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}
